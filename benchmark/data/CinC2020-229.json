{
  "paper_id": "CinC2020-229",
  "team_name": "UC_Lab_Kn",
  "authors_affiliation": "Lucas Weber, Maksym Gaiduk, Wilhelm Daniel Scherz, Ralf Seepold; HTWG Konstanz, Germany, and I.M. Sechenov First Moscow State Medical University, Moscow, Russian Federation",

  "result": {
    "official_challenge_metric_score": 0.27,
    "official_rank": 15,
    "evaluation_split": "official hidden test set (full test score 0.27); paper also reports a validation score of 0.656 and per-subset hidden scores (Set 1: 0.840, Set 2: 0.300, Set 3: 0.190)",
    "self_reported_metrics": {
      "f1": "not reported",
      "auroc": "not reported",
      "sensitivity": "not reported",
      "specificity": "not reported",
      "accuracy": "not reported",
      "other": "5-fold cross-validation on training data (not hidden test set): AUROC 0.943 (no augmentation) / 0.947 (with augmentation) / 0.970 (ensemble); AUPRC 0.567 / 0.577 / 0.611; Challenge metric 0.602 / 0.611 / 0.642 (ensemble). The ensemble was not evaluated on the official hidden test set due to runtime restrictions."
    }
  },

  "model": {
    "architecture_family": "ResNet",
    "architecture_details": "Modified 1D residual CNN: an initial block of three convolutions (the latter two with skip connections), followed by D=3 repeated groups of M=5 normal residual blocks (each pair of stride-1 convolutions plus a stride-3 bottleneck downsampling layer that doubles the feature count per repetition), then M additional normal blocks, a global max-pooling layer over each feature map (replacing intermediate pooling/batch norm), and a fully-connected classifier with one hidden layer that concatenates pooled convolutional features with patient age and sex, ending in a 24-class sigmoid output. ELU activations throughout; convolution kernel width W=5.",
    "key_novelty": "Removes intermediate pooling layers and batch normalization in favor of a single global max-pooling layer after the convolutional feature extractor, making the network fully independent of input signal length so it can train and predict on ECG segments of arbitrary duration, combined with five custom raw-signal data-augmentation methods (slicing, CutOut, channel shifting, additive noise, dropout)."
  },

  "input": {
    "leads_used": "12",
    "signal_length_window": "Variable length; training uses random slices up to 10000 samples (20s), minimum 4000 samples (or full signal if shorter); testing uses the whole signal (predictions averaged across slices if the signal exceeds 10000 samples) — architecture is signal-length independent via global pooling",
    "sampling_rate": "500 Hz (all signals resampled to 500 Hz via Fourier resampling from their original, varying sample rates)",
    "preprocessing": "Resampling to 500 Hz; division by each signal's gain value to obtain physical units; outlier removal via a 50-sample (0.1s) uniform-filter baseline, replacing samples deviating >2.5 mV from baseline with the signal mean, followed by a 5-sample (0.01s) median filter for smoothing; second-order Butterworth bandpass filter, cutoffs 0.5-45 Hz."
  },

  "data": {
    "training_datasets": "Full official PhysioNet/CinC2020 challenge training data (43101 signals); no external data reported; patient age and sex from the header (coded as -1 if missing) used as additional classifier inputs",
    "class_imbalance_handling": "not reported",
    "augmentation_techniques": "Signal slicing (variable-length random crops, up to 10000 samples); CutOut (per-lead masking with the signal mean, mask length 0 to 20% of channel length, applied with 35% probability per lead); channel shifting (circular shift, same amount for all channels up to 500 samples, or independently per channel up to 5 samples); additive white Gaussian noise (max amplitude 20 microvolts); Dropout (50%) in the fully-connected classifier."
  },

  "training": {
    "loss_function": "Binary cross-entropy",
    "optimizer": "Adam, learning rate 1e-4, weight decay 1e-5",
    "cross_validation_strategy": "5-fold cross-validation on training data for augmentation/ensemble comparisons; final submitted single model trained on a 90%/10% train/validation split; decision threshold of 0.071 (applied uniformly across classes) chosen by maximizing the challenge metric on the validation set",
    "hardware": "Tesla K40 GPU (Amazon Web Services); PyTorch framework; approximately 12 hours training time per model"
  },

  "contribution": {
    "summary": "Adapts a 1D residual CNN with a single global max-pooling layer to make the network invariant to variable ECG signal length, and shows that five lightweight raw-signal data-augmentation techniques (slicing, CutOut, channel shift, additive noise, dropout) improve cross-validated generalization performance over the same architecture trained without augmentation.",
    "code_available": "not reported",
    "code_url": "not reported"
  }
}
